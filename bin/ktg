#! /usr/bin/env python

import random
import requests

fact_type = random.choice(['cat', 'dog'])
response = requests.get('https://fact.birb.pw/api/v1/' + fact_type)
response_json = response.json()
print(response_json['string'])

