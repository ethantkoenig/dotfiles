#! /usr/bin/env bash

function run_command {
  echo "$(date): $@" >> $HOME/.async/log;
  $@ &>> $HOME/.async/log;
  if [[ $? -ne 0 ]]; then
    notify-send "async: $@ failed" 'See ~/.async/log';
  fi
}

run_command $@ &
